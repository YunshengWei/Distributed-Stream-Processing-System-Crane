import static org.junit.Assert.*;

import org.junit.Test;

public class GrepTest {

    @Test
    public void test() {
        fail("Not yet implemented");
        
    }

    /**Method to generate log files dynamically.
     * The log file will be generated such that they have very rare,rare,somewhat frequent
     * and frequent patterns
     */
     public void generateLogFile(){
         
     }
     /** Method to call the distributed log querier function to test the grep functionality
      * 
      */
     public void distributedLogQuerier()
     {
         
     }
     /**
      * Method to call grep command locally.
      */
     public void localLogQuerier()
     {
         
     }
     /**
      * Method to compare the output of the distributed grep and local grep
      */
     public void compareLogs()
     {
         
     }
}
